#!/usr/bin/env bash
set -euo pipefail

if command -v ccze >/dev/null 2>&1; then
  journalctl -u lightningd -f --no-pager --output=short-iso "$@" | ccze -A
else
  journalctl -u lightningd -f --no-pager --output=short-iso "$@"
fi

